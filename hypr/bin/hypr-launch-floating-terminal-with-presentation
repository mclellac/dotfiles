#!/bin/bash

set -e

source "$(dirname "$0")/hypr-utils"
check_deps alacritty hypr-show-logo hypr-show-done

setsid alacritty --class Hypr -e bash -c 'hypr-show-logo; eval "$1"; hypr-show-done' -- "$*"
