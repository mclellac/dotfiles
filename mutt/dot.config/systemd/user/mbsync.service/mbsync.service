[Unit]
Description=Mailbox synchronization service

[Service]
Type=oneshot
# Ensure the path matches your mbsync binary (usually /usr/bin/mbsync)
ExecStart=/usr/bin/mbsync -a -c /home/mclellac/.config/isync/mbsyncrc
ExecStartPost=/usr/bin/notmuch new
