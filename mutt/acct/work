# Work Account
# ------------------------------------------------------------------------------
# set imap_check_subscribed
# set imap_user 	= "user@domain"
# set imap_pass 	= ""
set folder 	    = "~/.local/share/mail/work"
# set smtp_url	= "smtps://user@domain@smtp.gmail.com/465"
# set smtp_pass 	= ""
set from 	    = "user@domain"
set realname 	= "real name"

set spoolfile 	= "+INBOX"
# set postponed 	= "+[Gmail]/Drafts"
set postponed = "+Drafts"
set trash		= "+[Gmail]/Trash"
set my_spam_folder = "+[Gmail]/Spam"
set my_sent_folder = "+[Gmail]/Sent Mail"
set ispell      = "aspell --mode=email check"
set include=yes

set header_cache        =~/.cache/mutt/work/headers
set message_cachedir 	=~/.cache/mutt/work/bodies
set certificate_file 	=~/.config/mutt/certificates/work

# Neomutt needs to be told which mailboxes to check for new mail.
# We use a dynamic python script to find all local mailboxes robustly.
# This handles spaces and hierarchy correctly.
# ---------------------------------------------------------------------------------------------------
mailboxes `python3 ~/.config/mutt/scripts/find_mailboxes.py "$HOME/.local/share/mail/work"`
